Used for creating family of objects. In Factory method pattern different concrete objects of same type gets created based on the context. 
But in abstract factory pattern, we create several concrete objects using factory and there will be several factories. 

Here, GUIFactory is an abstract factory and MacOsFactory and WindowsFactory are concrete factories. 
Each factory generates family of objects namely, buttons and checkboxes. 

